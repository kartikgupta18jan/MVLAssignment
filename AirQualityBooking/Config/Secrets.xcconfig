// LOCAL ONLY — git-ignored, never committed.
// Injected at build time: xcconfig → build setting → Info.plist → AppConfiguration.swift
// Leave blank to run in fully-mocked mode (no token needed for reviewer).

AQICN_API_TOKEN = edd402d708393c32c12ae6f216fb0a4143a2be14
